import { EntityTarget, ObjectLiteral } from "typeorm";

export const BASE_SERVICE = 'BASE_SERVICE';

export interface IBaseService {

    // createEntity(entity?: EntityTarget<ObjectLiteral>): Promise<EntityTarget<ObjectLiteral>>;
    
}